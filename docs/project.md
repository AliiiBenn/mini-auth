

Cette librairie permet de gérer l'authentification pour n'importe quelle application Python. 
- Gérer des utilisateurs en fonction d'une table SQL. (PostgreSQL, SQLite, MySQL, etc.)
- On peut s'authentifier avec email-password