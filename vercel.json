{
  "devCommand": "uvicorn src.main:app --host 0.0.0.0 --port 3000 --reload",
  "builds": [
    {
      "src": "api/app.py",
      "use": "@vercel/python"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "api/app.py"
    }
  ]
} 